<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="CustomBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">@color/arrow_color</item>
    </style>



    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CustomCheckboxTheme" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name = "android:button">@drawable/checkbox_style</item>
    </style>

    <!-- Inventory row style -->
    <style name="inventoryRowItemName">
        <item name="android:textSize">16dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
    </style>

    <style name="inventoryRowItemDays">
        <item name="android:textSize">12dp</item>
        <item name="android:textColor">@color/bayInfo</item>
    </style>

    <style name="inventoryRowItemWarningDays" parent="inventoryRowItemDays">
        <item name="android:textColor">@color/bayWarning</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="inventoryRowItemExpiredDays" parent="inventoryRowItemDays">
        <item name="android:textColor">@color/bayError</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Hamburger menu style -->
    <style name="userNameText">
        <item name="android:textSize">16dp</item>
        <item name="android:textColor">@color/bayWhite</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="userEmailText">
        <item name="android:textSize">14dp</item>
        <item name="android:textColor">@color/bayWhite</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="drawerItemText">
        <item name="android:textSize">16dp</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
